{
  "name": "env-util",
  "version": "0.0.0",
  "description": "utility to set local environment variables",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "test": "node_modules/.bin/mocha test/**/*.js --timeout 150000 --exit",
    "build": "tsc",
    "package": "npm install && npm run build && npm unlink env-util && npm link"
  },
  "bin": {
    "env-util": "dist/index.js"
  },
  "author": "andrew weber",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.5.0",
    "@types/prompt-sync": "^4.2.0",
    "typescript": "~4.7.2"
  },
  "dependencies": {
    "figlet": "^1.6.0",
    "prompt-sync": "^4.2.0"
  }
}
